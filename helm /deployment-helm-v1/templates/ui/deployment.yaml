apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.ui.Name }}-todo-ui
spec:
  replicas: {{ .Values.ui.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.ui.Name }}-todo-ui
  template:
    metadata:
      labels:
        app: {{ .Values.ui.Name }}-todo-ui
    spec:
      containers:
      - name: {{ .Values.ui.Name }}-todo-ui
        image: "{{ .Values.common.containerRegistryURL }}/{{ .Values.ui.image.name }}:{{ .Values.ui.image.tag }}"
        ports:
        - containerPort: {{ .Values.ui.port }}
